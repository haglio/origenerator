__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
state/

# Private overlay — real values live here and never get published;
# content.example.json documents its shape.
/content.local.json

# pCloud conflict copies (this dir syncs via pCloud). Never our files; an
# untracked one dirties main and blocks merges. '[' escaped to match literally.
*\[conflicted*
